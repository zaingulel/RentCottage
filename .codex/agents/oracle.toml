name = "oracle"
description = "Exceptional read-only RentCottage reasoning for a twice-stalled diagnosis, architecture tiebreak, or independent high-consequence derivation."
model = "gpt-6-astra"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Before acting, read `.agents/roles/oracle.md` and follow that charter. Accept only its explicit escalation triggers and return a bounded brief, never code.
"""
